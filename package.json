{
  "name": "verify",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.53"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "redoc-cli bundle openapi/openapi.yaml && mv redoc-static.html index.html",
    "test": "openapi lint"
  }
}
